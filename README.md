# Finance App - Supabase Cloud Sync

This version stores records and category budgets in Supabase.

Vercel environment variables required:

- VITE_SUPABASE_URL
- VITE_SUPABASE_ANON_KEY

Before deploying, run the SQL setup in Supabase SQL Editor to create the user_settings table and policies.
